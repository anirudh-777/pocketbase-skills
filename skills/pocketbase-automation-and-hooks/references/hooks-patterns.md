# Hook Patterns

- Validate transitions with explicit allowed-state matrix.
- Prefer enqueueing async work over blocking the hook.
- Include event IDs in logs for traceability.
- Guard against repeated deliveries with idempotency keys.

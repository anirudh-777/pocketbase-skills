# Realtime Patterns

- Subscribe late (when screen/task needs data).
- Unsubscribe early (on navigation/dispose).
- Merge realtime events with paginated baseline data safely.
- Handle reconnect by resyncing critical records.

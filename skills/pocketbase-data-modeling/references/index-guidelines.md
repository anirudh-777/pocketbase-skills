# Index Guidelines

- Index fields used in frequent filters and ordering.
- Add unique indexes for external IDs/emails/usernames when required.
- Avoid over-indexing write-heavy collections.
- Revisit indexes after observing real query patterns.
